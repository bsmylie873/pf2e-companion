# Backend API base URL (required for build)
VITE_API_BASE_URL=http://localhost:8080

# --- localStorage keys (documentation only) ---
# pf2e-dark-mode    — stores "true" | "false" for dark mode preference
# pf2e-layout-pref  — stores "grid" | "list" for games list layout
